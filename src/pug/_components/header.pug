header.header.jq-header
  ._container
    transition(name='fade')
      nav.nav(v-show="visible")
        ul.nav__list
          li.nav__item
            a.nav__link(href='/') Главная
          each link in links
            li.nav__item
              a.nav__link(href=`${link.path}`)=link.name
    button(@click="visible = !visible") {{ visible ? 'Скрыть меню' : 'Показать меню' }}
    | Шрифт
    button(@click="mainFontSizeMin") -
    button(@click="mainFontSizeMax") +
