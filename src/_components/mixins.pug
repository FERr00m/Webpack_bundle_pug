mixin icon(name, mod)
  - mod = mod || ''
  if mod
    svg(class=`icon icon-${name} ${mod}`)
      use(xlink:href=`img/sprite.svg#${name}`)
  else
    svg(class=`icon icon-${name}`)
      use(xlink:href=`img/sprite.svg#${name}`)
